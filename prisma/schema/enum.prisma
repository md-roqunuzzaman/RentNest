enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum UserStatus {
    ACTIVE
    BANNED
}

enum PropertyType {
    APARTMENT
    HOUSE
    STUDIO
    DUPLEX
    VILLA
    PENTHOUSE
    OFFICE
    SHOP
}

enum RentalStatus {
    PENDING
    APPROVED
    REJECTED
    ACTIVE
    COMPLETED
}

enum PaymentProvider {
    STRIPE
}

enum PaymentMethod {
    CARD
}

enum PaymentStatus {
    PENDING
    COMPLETED
    FAILED
}
